// Conversão de polegada em centímetros
function converte(x,y) {
return x * 2.54;
}
converte(5);
console.log(converte(5)) // Mostra no terminal o resultado

// Converter uma string em uma url
function url(google) {
    return "https://www.google.com.br"
}
url()
console.log(url()) // Mostra no terminal o resultado

